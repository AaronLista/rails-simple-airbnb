<h1>All our flats</h1>
<p>All (<%= @flats.size %>) flats</p>

<div class="row">
  <% @flats.each do |flat| %>
    <div class="col-4 mb-3">
      <%= render "flat", flat: flat %>
    </div>
  <% end %>
</div>
